{
  "name": "crawl-pwa",
  "version": "0.1.0",
  "private": true,
  "description": "Mobile-friendly PWA client for Dungeon Crawl Stone Soup WebTiles servers.",
  "type": "module",
  "scripts": {
    "dev": "node dev-server.mjs",
    "deploy": "wrangler pages deploy .",
    "deploy:preview": "wrangler pages deploy . --branch=preview"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
